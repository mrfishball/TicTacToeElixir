defmodule MessageFlags do

  @menu :menu
  @error :error
  @status :status
  @request :request

  def menu do
    @menu
  end

  def error do
    @error
  end

  def status do
    @status
  end

  def request do
    @request
  end
end
